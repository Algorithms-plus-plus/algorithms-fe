import { AccordionDropdownDirective } from './accordion-dropdown-directive';

describe('AccordionDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
